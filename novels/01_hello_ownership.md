# 01: 最初のスキルと所有権の移動

対応教材: [Level 1: Rust に触る](../levels/level_01_intro/README.md)

## 物語パート

洞窟で目覚めて数日。

俺は、転がる、跳ねる、染み込む、というスライムらしい移動に慣れてきた。だが、いまだに自分が何者なのかを他者へ伝える方法がない。

「名乗りたい」

『告。名乗りは最小出力です。標準的には `println!` を使用します』

「異世界で名乗るのに、標準的とかあるのか」

『解。`println!` は文字列を標準出力へ表示するマクロです』

「マクロ？」

『保留。現時点では、便利な出力呪文と理解してください』

俺は魔素を集中させた。洞窟の壁に文字が浮かぶ。

```rust
fn main() {
    println!("Hello, Rust!");
}
```

「おお、出た。俺、プログラムになった？」

『否定。プログラムが出力を行いました。個体はまだスライムです』

「そこは乗ってくれてもいいだろ」

洞窟の奥から、低い笑い声が響いた。

「クワッハッハ！ 面白いスライムだな。名を持つ前に出力を覚えるとは！」

巨大な竜が、封印の中からこちらを見ていた。

「誰だ、あんた」

「我は暴風竜。長く封じられておる。貴様、名は？」

名前。所有するもの。自分の責任で名乗るもの。

俺は少し考えた。

「まだない。けど、いまから作る」

## 会話・独白パート

俺は大賢者に聞いた。

「文字列って、どうやって持つんだ？」

『解。所有する文字列には `String` を使用します。借りるだけの文字列には `&str` を使用します』

「所有する名前と、借りる名前が違うってこと？」

『肯定。例えば、名札を作って持つなら `String`。誰かの名札を読むだけなら `&str` です』

暴風竜が鼻を鳴らす。

「名前とは魂に刻むものだ。借り物ではつまらぬぞ」

「いや、API の話をしてるんだ」

『提案。まず小さな例で確認します』

```rust
fn main() {
    let name = String::from("Rust");
    borrow_name(&name);
    take_name(name);
}

fn borrow_name(value: &str) {
    println!("borrowed: {}", value);
}

fn take_name(value: String) {
    println!("owned: {}", value);
}
```

「`borrow_name(&name)` は、名前を貸してるだけ？」

『解。はい。`name` の所有者は `main` のままです』

「`take_name(name)` は？」

『解。`String` の所有権が `take_name` に移動します。その後、元の `name` は使えません』

暴風竜が楽しそうに笑った。

「なるほど。名を渡せば、そやつのものになる。名を見せるだけなら、自分のもののままか」

「そう言われると、異世界っぽいな」

## 大賢者による解説

『告。Level 1 の目的は、Rust を動かし、所有権の移動を体験することです』

`String` は、ヒープに確保された文字列データを所有します。関数に `String` をそのまま渡すと、所有権が移動します。

`&str` は文字列スライスです。文字列を所有せず、既存の文字列を読むための参照として使います。

『解。関数の引数型は、その関数が値を所有したいのか、読むだけでよいのかを表します』

悪い癖は、コンパイルエラーを避けるためだけに `clone()` を使うことです。`clone()` は所有するデータを複製します。必要な場面では正しい判断ですが、理由なく使うと責任範囲が曖昧になります。

```rust
fn print_name(value: &str) {
    println!("{}", value);
}
```

表示するだけなら、所有権を受け取る必要はありません。

## Rust 任務

実行するもの:

```bash
rustc --edition=2021 levels/level_01_intro/examples/hello_ownership.rs -o /tmp/zero_to_rust_hello
/tmp/zero_to_rust_hello
```

期待する出力:

```text
borrowed: Rust
owned: Rust
```

観察すること:

```text
borrowed は借用で表示している。
owned は所有権を受け取って表示している。
同じ表示でも、関数の契約が違う。
```

## 初出用語・関数の説明

- `fn main()`: Rust プログラムの入口になる関数。
- `println!`: 標準出力に文字列を表示するマクロ。末尾に改行を付ける。
- `let`: 変数束縛を作るキーワード。
- `String`: 伸長可能で所有権を持つ文字列型。
- `String::from`: 文字列リテラルなどから `String` を作る関連関数。
- `&str`: 文字列を所有せずに読むための文字列スライス。
- `&name`: `name` を借用する表記。
- `clone()`: 値を複製するメソッド。設計理由がある場合に使う。

## 進級チェック

次の問いに答えられたら、次章へ進む。

```text
String と &str の違いを説明できるか。
関数に String を渡すと何が起きるか説明できるか。
表示するだけの関数が &str を受け取る理由を説明できるか。
clone を使う前に、なぜ複製が必要か考えられるか。
```

暴風竜は、封印の中で満足げに目を細めた。

「スライムよ。貴様、なかなか見どころがある」

「そうか？」

『告。肯定できます。ただし、次は失敗を扱う必要があります』

「名乗ったばかりで失敗の話かよ」

『Rust では避けられません』
