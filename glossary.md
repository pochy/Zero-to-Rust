# Glossary

Rust を学ぶときに最初に必要な言葉を、実務判断に結びつく形で整理します。

## 所有権

値を最後まで責任を持って管理する権利です。`String` を関数にそのまま渡すと、所有権も移動します。

```rust
fn take(value: String) {}
```

## 借用

所有権を渡さず、一時的に値を使わせることです。読み取りだけなら `&T`、変更も許すなら `&mut T` を使います。

## ライフタイム

参照がどこまで有効かを表す考え方です。初心者は記号として覚えるより、「この参照は何に依存しているか」と考えると理解しやすくなります。

## `String`

文字列データを所有する型です。長さが変わる文字列や、関数から返す文字列に向いています。

## `&str`

文字列の一部または全体を借りて見る型です。関数が文字列を読むだけなら、まず `&str` を検討します。

## `Vec<T>`

同じ型の値を順番に持つ可変長配列です。要素を追加したり、順番に処理したりするときに使います。

## `HashMap<K, V>`

キーから値を探すための標準コレクションです。KVS、集計、頻度表などに使います。

## `Option<T>`

値があるかもしれないし、ないかもしれないことを表します。存在しない状態を `null` ではなく型で表現します。

## `Result<T, E>`

成功か失敗かを表します。成功なら `T`、失敗なら `E` を持ちます。Rust では失敗も設計の一部です。

## `?`

`Result` や `Option` の失敗側を、呼び出し元へ早く返す演算子です。エラーを握りつぶさずに伝播するときに使います。

## `unwrap()`

成功値を強制的に取り出し、失敗なら panic するメソッドです。学習初期の実験では便利ですが、実務コードでは根拠が必要です。

## `struct`

複数の値を 1 つの意味あるデータとしてまとめる仕組みです。Rust では、データの所有境界を表す重要な道具です。

## `enum`

取りうる状態の種類を表す仕組みです。コマンド、レスポンス、エラー、状態遷移を型で表現できます。

## trait

型が提供する振る舞いの約束です。変更される軸が見えてから導入すると、設計が明確になります。

## `Box<T>`

値をヒープに置き、所有権を 1 つだけ持つスマートポインタです。再帰的データ構造などで使います。

## `Rc<T>`

単一スレッド内で、複数の所有者を許す参照カウント型です。スレッドをまたぐ共有には使えません。

## `Arc<T>`

複数スレッド間で共有できる参照カウント型です。共有するだけで中身を変更できるわけではありません。

## `Mutex<T>`

同時に 1 つのスレッドだけが中身を変更できるようにするロックです。ロック範囲を短く保つことが重要です。

## `RwLock<T>`

複数の読み取り、または 1 つの書き込みを許すロックです。読み取りが多い設計で候補になります。

## WAL

Write-Ahead Log の略です。状態を直接保存する前に操作ログを追記し、障害後にログを再生して復旧する考え方です。

## プロトコル

通信で使う約束です。たとえば `SET key value` を受け取ったら `OK` を返す、というようにリクエストとレスポンスの形を定めます。

## 可観測性

システムの状態を外から確認できる性質です。ログ、メトリクス、health check などで実現します。
